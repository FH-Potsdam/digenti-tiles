var config = {};

// Tile Server Settings
config.server = {};
config.server.port = 10060; // https://wiki.uberspace.de/system:ports

// Tile Settings
config.tile = 'ndvi_gradient.mbtiles';

module.exports = config;
