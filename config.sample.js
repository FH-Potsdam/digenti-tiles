var config = {};

// Tile Server Settings
config.server = {};
config.server.host = 'localhost';
config.server.port = 61001;

// Tile Settings
config.tile = 'YOUR_MAPBOX_TILE.mbtiles';

module.exports = config;
